import {
    Root
}
from '@radix-ui/react-tabs';

import type {
    TabsProps
}
from '@radix-ui/react-tabs';

export const Picker: React.FC<TabsProps> = (prop) => {
    return (
        null
    );
};

<Picker />
